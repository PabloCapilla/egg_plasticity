##
## setup renv for the project

# if not installed: install.packages('renv')
# see https://rstudio.github.io/renv/index.html for instructions to reproduce the R environment in which this project took place

renv::init()
